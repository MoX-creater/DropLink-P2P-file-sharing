# Signaling server WebSocket URL.
# Must be prefixed with VITE_ so Vite exposes it to browser code.
# Use ws:// for local dev; wss:// for production (TLS required for WebRTC in most browsers).
VITE_SIGNALING_URL=ws://localhost:3001
